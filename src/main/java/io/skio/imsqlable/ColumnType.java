package io.skio.imsqlable;

public enum ColumnType {
  BINARY,
  BOOLEAN,
  DATE,
  DATETIME,
  DECIMAL,
  FLOAT,
  INTEGER,
  BIGINT,
  PRIMARY_KEY,
  REFERENCES,
  STRING,
  TEXT,
  TIME,
  TIMESTAMP,
  INFERRED
}
